# Rendered runtime env files (contain decrypted secrets) — never commit.
env/*.runtime.env

# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
!terraform/*.tfvars.example
terraform/crash.log

# Local env overrides
.env
*.local

# Claude Code workspace
.claude/

# OS
.DS_Store
Thumbs.db
